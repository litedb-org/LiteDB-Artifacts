# Selected unchanged synthetic evidence

Only explicitly allowlisted fuzz inputs, replay descriptors and run metadata are published. All are synthetic; machine-path/token screening and exact seeded-word verification passed. See ../README.md and fuzz/README.md for provenance and replay. No database snapshots, logs, executable binaries, PDBs or process dumps are included.
